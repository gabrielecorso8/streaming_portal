# Virtual environment
venv/

# Local cache and temporary files
__pycache__/
*.pyc
tmp/

# Downloaded media, logs, and runtime state (not shared)
downloads/
*.log
downloads_state.json
library_state.json

# Local binaries (ffmpeg) — too large for the repo
bin/

# PyInstaller build output (l'eseguibile si rigenera con "Crea EXE.bat")
build/
dist/
*.exe

# File locali dell'agente (non vanno nel repo)
.claude/
.agents/
.codex/

# Backup locali della migrazione link
*.bak_link_migration
*.bak_nullfix
*.bak_truncated
static/hls.min.js

# Segreti/impostazioni locali: settings.json contiene il TOKEN di accesso -> MAI nel repo
settings.json

# NB: library.json (libreria + preferiti) e covers/ (locandine attuali) sono INCLUSI
#     di proposito nel repository.

# Config/segreti locali
.env
.env.*

# Note di sviluppo interne (handoff), non nel repo pubblico
CLAUDE.md
